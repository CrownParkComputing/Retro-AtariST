#pragma once

// Copy this file to LocalCredentials.h for a personal, non-distributed build.
// The real file is gitignored. Prefer signing in through the app for release
// builds so a password is never embedded in the application binary.
#define RETRO_ATARIST_BOOTSTRAP_EMAIL "you@example.com"
#define RETRO_ATARIST_BOOTSTRAP_PASSWORD "replace-me"
